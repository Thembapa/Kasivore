import waitress
from bottle import Bottle

app =Bottle()

@app.route('/hello')
@app.route('/hello/<a>')
def echo(a = None):
	if a == None:
		return('nada')
	else:
		print(a)
		return (a)


waitress.serve(app, listen='*:8765')